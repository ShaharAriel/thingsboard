
bridge:
  username: "${BRIDGE_USER:rokeha_user@gmail.com}"
  password: "${BRIDGE_PASS:12345678}"
  schema: "${BRIDGE_BIND_SCHEMA:http}"
  host: "${BRIDGE_BIND_ADDRESS:127.0.0.1}"
  port: "${BRIDGE_BIND_PORT:8080}"